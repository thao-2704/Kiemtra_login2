<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke
        android:width="1dp"
        android:color="@color/nut">
    </stroke>

    <!-- các thuộc tính căn chỉnh-->
    <padding
        android:left="5dp"
        android:top="5dp"
        android:right="5dp"
        android:bottom="5dp" >
    </padding>
    <solid
        android:color="@color/nut"/>

    <!-- và đây là bán kính đường tròn ở 4 góc -->
    <corners
        android:radius="7dp" >
    </corners>
</shape>